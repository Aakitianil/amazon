import { ErrorStyleDirective } from './error-style.directive';

describe('ErrorStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new ErrorStyleDirective();
    expect(directive).toBeTruthy();
  });
});
